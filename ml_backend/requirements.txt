# SENTINEL v3 — Python ML Backend
# Install: pip install -r requirements.txt

flask==3.0.0
flask-cors==4.0.0
scikit-learn==1.3.2
numpy==1.26.2
pandas==2.1.3
requests==2.31.0
xgboost==2.0.2        # optional — remove if install fails; GradientBoostingClassifier is the fallback
gunicorn==21.2.0
